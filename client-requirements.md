# note.db 汎用クライアント 要件定義

## 概要

note.db フォーマットに準拠した SQLite ファイルを読み込み、CRUD 操作ができる静的 Web アプリケーション。すべての処理はブラウザ内で完結し、サーバーへのデータ送信は行わない。

## 技術方針

- 静的配信（GitHub Pages / Cloudflare Pages 等）
- WASM-SQLite でブラウザ内 SQLite を実現
- フレームワークは未定（React+Vite）

---

## 機能要件

### 1. データの読み込み

ローカルファイルと URL の2系統で DB を準備できる。

#### 1.1 ローカル SQLite ファイル

- ユーザーがローカルの SQLite ファイルをブラウザで開く（ファイル選択 or ドラッグ&ドロップ）
- ファイルをメモリに読み込み、WASM-SQLite で操作する
- ファイルへの書き戻し（保存）も対応する

#### 1.2 URL からのフォーマット取り込み

note.db フォーマットの `format.sql` を URL 指定で取得し、DB に適用する。フォーマット（例: `zettelkasten-v1`）の「インストール」操作にあたる。

- 入力 UI: URL 入力フォームを設ける（FileOpener / 読み込み済み画面の両方からアクセス可能）
- URL 制限: GitHub のみ
  - `https://github.com/<owner>/<repo>/blob/<ref>/<path>`
  - `https://raw.githubusercontent.com/<owner>/<repo>/<ref>/<path>`
  - `blob/` 形式は raw URL に自動変換する
- 振る舞いは現在の DB 状態で分岐:
  - **DB 未読み込み**: 空の in-memory DB を作り、取得した SQL を実行 → そのまま「読み込み済み DB」として扱う
  - **DB 読み込み済み**: 現在の DB に対して SQL を実行（新フォーマットのテーブル群が追加される）
- SQL 実行はトランザクションで囲み、エラー時はロールバックする
- 成功後はサイドバーが自動で更新される
- 保存はローカルファイル保存と同じ責務（Phase 2）。本機能はメモリ上の適用までを担う

note.db のプレフィックス規約は publish するフォーマット向けの推奨であり、refill 側で「フォーマット名」を抽出して UI に出すことはしない。refill は規約に依存しない動作を目指す。

### 2. テーブル・ビューの認識

ファイルを開くと自動的に以下を検出しサイドバーに表示する。

- ノートテーブル: `id`, `title`, `body`, `created_at`, `updated_at` を持つテーブル
- ビュー: SQLite の VIEW として定義されているもの

サイドバー表示はノートテーブルとビューをセクションで分ける（ノートテーブルが上、ビューが下）。

### 3. レコード一覧

ノートテーブルを選択すると、レコードのタイトルを一覧表示する。

- ソート順: `updated_at` 降順
- 表示項目: `title`、`updated_at`（補助情報として）

### 4. レコード閲覧・編集

レコードを選択すると閲覧・編集画面が開く。

#### エディタ

- `body` カラムを Obsidian ライクな Markdown エディタで表示・編集
- 閲覧モード（Markdown レンダリング）と編集モードの切り替えが可能

#### プロパティパネル

- `id`, `title`, `body`, `created_at`, `updated_at` 以外のカラムをサイドパネルにプロパティとして表示（Obsidian の front-matter 相当）
- 各プロパティはインライン編集可能
- すべて nullable なため、値なし（NULL）の状態を表現できること

#### 保存

- 編集内容は `updated_at` を現在時刻に更新して保存する

### 5. 新規レコード作成

- ノートテーブルを選択した状態で新規作成ボタンを押すと、空のレコードを作成して編集画面を開く
- `id`: UUID を自動生成
- `created_at`, `updated_at`: 現在時刻を自動設定
- `title` のみ必須入力、他は任意

### 6. レコード削除

- 編集画面またはレコード一覧から削除できる
- 削除前に確認ダイアログを表示する

### 7. 関連テーブルの編集

ノートテーブルの `id` を外部キーとして参照しているテーブルを認識し、ノートの編集画面からサブ編集フォーム（ダイアログまたはサイドバー）で操作できる。

- ノートを開くと、そのノートの `id` を FK として持つテーブルのレコードをサブフォームで一覧表示
- サブフォームからレコードの追加・編集・削除が可能
- テーブルは選択式（複数の関連テーブルがある場合はタブまたはドロップダウンで切り替え）
- FK カラムは自動的に現在のノートの `id` で埋められる

---

## 非機能要件

- 完全クライアントサイド処理（外部 API 通信なし）
- 特定のフォーマット（refill-v1 等）に最適化しない
- ビューは「読み取り専用のリスト表示」のみ（ビューに対する CRUD は行わない）

---

## 開発フェーズ

| Phase | 内容 |
|-------|------|
| 1 | データ読み込み（ローカルファイル / URL からのフォーマット取り込み）・テーブル/ビュー認識・レコード一覧表示（読み取り専用） |
| 2 | レコード編集・作成・削除（CRUD） |
| 3 | 関連テーブルのサブ編集フォーム |
| 4 | ビューの表示対応 |

---

## note.db 規約との対応

このクライアントは note.db の以下の規約に依存する。

- ノートテーブルの必須カラム（id, title, body, created_at, updated_at）— 検出ロジックの判定基準
- 規約外カラムはすべて nullable — プロパティパネルが NULL を表現できることが前提
- プレフィックス規約には**依存しない**。テーブル/ビューの検出はカラム構成のみで行い、テーブル名のパターンマッチはしない
